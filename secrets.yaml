# This file contains sensitive credentials and should be gitignored.
# Use this with: helm upgrade --install ... -f values-dev.yaml -f secrets.yaml

oracleAuth:
  enabled: true
  # Secret name to create
  secretName: oracle-auth
  # Credentials from oci-auth/external-secret/oracle-auth.yaml
  fingerprint:
  privatekey:
