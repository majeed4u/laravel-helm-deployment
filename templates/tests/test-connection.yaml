apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laravel_backend_helm.fullname" . }}-test-connection"
  labels:
    {{- include "laravel_backend_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laravel_backend_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
